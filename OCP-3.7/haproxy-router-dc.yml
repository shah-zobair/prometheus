apiVersion: v1
kind: DeploymentConfig
metadata:
  name: haproxy-exporter-router
  labels:
    app: haproxy-exporter
spec:
  strategy:
    type: Rolling
    rollingParams:
      updatePeriodSeconds: 1
      intervalSeconds: 1
      timeoutSeconds: 600
      maxUnavailable: 25%
      maxSurge: 25%
    resources: {}
  triggers:
    - type: ConfigChange
    - type: ImageChange
      imageChangeParams:
        automatic: true
        containerNames:
          - haproxy-exporter
        from:
          kind: ImageStreamTag
          namespace: openshift
          name: 'haproxy-exporter:v3.7'
  replicas: 1
  test: false
  selector:
    app: haproxy-exporter
    deploymentconfig: haproxy-exporter-router
  template:
    metadata:
      labels:
        app: haproxy-exporter
        deploymentconfig: haproxy-exporter-router
      annotations:
        openshift.io/generated-by: OpenShiftWebConsole
    spec:
      containers:
        - name: haproxy-exporter
          image: >-
            docker-registry.default.svc:5000/openshift/haproxy-exporter:v3.7
          args:
            - >-
              --haproxy.scrape-uri=http://admin:HHfgQ4rgMx@router.default.svc.cluster.local:1936/haproxy?stats;csv
          ports:
            - containerPort: 9101
              protocol: TCP
          resources: {}
          terminationMessagePath: /dev/termination-log
          imagePullPolicy: Always
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      dnsPolicy: ClusterFirst
      securityContext: {}
